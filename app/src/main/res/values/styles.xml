<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="AppTheme" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/my_color</item>
        <item name="colorPrimaryDark">@color/lime_color</item>
        <item name="colorAccent">@color/lime_coloraccent</item>

        <!-- Add the following item to use the toolbar -->
        <item name="toolbarStyle">@style/AppTheme.Toolbar</item>
    </style>

    <style name="AppTheme.Toolbar" parent="Widget.MaterialComponents.Toolbar.Primary">
        <item name="titleTextColor">@android:color/white</item>
    </style>

</resources>